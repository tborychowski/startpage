@import "_lib";

boxHeight = 80;

.tile-settings
	pos 0 0 0 0; top auto; bg sidebar-bg; color #fff; padding 10; height boxHeight;
	transition transform .3s ease-out; transform translateY(px(boxHeight));

	&.expanded
		transform translateY(0);


body.tile-settings-expanded .main
	bottom boxHeight;
